{
  "name": "backlog-cli",
  "scripts": {
    "start": "bun ./src/index.ts",
    "compile": "bun build ./src/index.ts --compile --outfile ./out/$npm_package_name"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "imports": {
    "#*": "./src/*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "citty": "^0.1.6"
  }
}
