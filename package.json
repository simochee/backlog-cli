{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "type-checks": "turbo type-checks"
  },
  "packageManager": "bun@1.3.5",
  "devDependencies": {
    "turbo": "^2.7.0"
  }
}