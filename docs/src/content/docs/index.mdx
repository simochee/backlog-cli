---
title: Backlog CLI
description: Backlog をコマンドラインから操作する非公式 CLI ツール
template: splash
hero:
  title: 開発のリズムを、止めない。
  tagline: 課題の確認、PR のマージ、通知のチェック — ブラウザに切り替えることなく、ターミナルから Backlog のすべてを操作できる非公式 CLI ツール。
  image:
    file: ../../assets/logo.svg
    alt: Backlog CLI
  actions:
    - text: はじめる
      link: /getting-started/installation/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/simochee/backlog-cli
      icon: external
      variant: minimal
---

import { Aside, Card, CardGrid, LinkCard, Steps, Tabs, TabItem } from "@astrojs/starlight/components";

<Aside type="note">
	このツールは非公式のコミュニティプロジェクトであり、[ヌーラボ社](https://nulab.com/)および
	[Backlog](https://backlog.com/) チームとは一切関係がありません。
</Aside>

## 特徴

<CardGrid>
	<Card title="Backlog API をフルカバー" icon="rocket">
		課題、プルリクエスト、Wiki、通知、Webhook、チーム管理まで — 99 のコマンドで Backlog のほぼすべての機能にアクセスできます。
	</Card>
	<Card title="gh CLI ライクなインターフェース" icon="laptop">
		GitHub CLI に慣れた開発者ならすぐに使いこなせる、直感的なコマンド体系。`list`・`view`・`create`・`edit` の一貫した操作感で迷いません。
	</Card>
	<Card title="スクリプタブルな出力" icon="setting">
		`--json` フラグでプログラムから扱いやすい JSON を出力。`jq` との組み合わせや CI/CD パイプラインへの組み込みもスムーズです。
	</Card>
	<Card title="AI エージェント連携" icon="star">
		Agent Skill を導入すれば、Claude Code や Cursor などの AI エージェントに自然言語で Backlog 操作を指示できます。
	</Card>
</CardGrid>

## こんな使い方ができます

### 課題を一覧表示する

```bash
backlog issue list --project MY_PROJECT --assignee @me
```

```
#    種別    優先度  担当者        件名
123  タスク  中      yamada        API エンドポイントの設計
124  バグ    高      tanaka        ログインページの表示崩れ
125  タスク  低      suzuki        README の更新
```

### 課題を作成する

```bash
backlog issue create --project MY_PROJECT \
  --title "新機能: ダークモード対応" \
  --type タスク \
  --priority 中 \
  --assignee yamada
```

### プルリクエストの状態を確認する

```bash
backlog pr status --project MY_PROJECT --repo my-app
```

```
オープン
#42  ダークモード対応       feature/dark-mode → main
#39  パフォーマンス改善     fix/perf → main

マージ済み
#38  認証フローのリファクタ refactor/auth → main
```

### ダッシュボードで全体を把握する

```bash
backlog status
```

```
自分の課題 (3件)
  MY_PROJECT-123  API エンドポイントの設計     処理中
  MY_PROJECT-130  テストカバレッジの改善       未対応
  MY_PROJECT-142  ドキュメントの整備           未対応

未読通知 (2件)
  MY_PROJECT-124  tanaka がコメントしました
  MY_PROJECT-138  PR がマージされました
```

### JSON 出力でスクリプトと連携する

```bash
# 高優先度の課題だけを抽出
backlog issue list --project MY_PROJECT --json | jq '[.[] | select(.priority.name == "高")]'

# 課題キーの一覧を取得してループ処理
backlog issue list --project MY_PROJECT --json | jq -r '.[].issueKey' | while read key; do
  echo "Processing $key"
done
```

### AI エージェントから使う

[skills CLI](https://www.npmjs.com/package/skills) で AI エージェント向けのスキルを追加すると、自然言語で Backlog 操作を指示できます。

```bash
npx skills add simochee/backlog-cli
```

「PROJ-123 を処理中にして」「自分の未完了課題を一覧して」といった自然言語の指示を、エージェントが適切な `backlog` コマンドに変換して実行します。詳しくは [AI エージェント連携ガイド](/guides/ai-agent/) をご覧ください。

## クイックスタート

<Steps>

1. **インストール**

   ```bash
   npx nypm install -g @simochee/backlog-cli
   ```

2. **認証**

   ```bash
   backlog auth login
   ```

   対話形式でホスト名と認証方式（API キーまたは OAuth）を設定します。

3. **使ってみる**

   ```bash
   backlog issue list --project YOUR_PROJECT
   ```

</Steps>

## さらに詳しく

<CardGrid>
	<LinkCard
		title="インストール"
		href="/getting-started/installation/"
		description="パッケージマネージャーごとのインストール手順"
	/>
	<LinkCard
		title="クイックスタート"
		href="/getting-started/quickstart/"
		description="認証の設定と基本的なコマンドの使い方"
	/>
	<LinkCard title="コマンドリファレンス" href="/commands/auth/" description="全 99 コマンドの詳細なリファレンス" />
	<LinkCard
		title="AI エージェント連携"
		href="/guides/ai-agent/"
		description="Claude Code や Cursor から Backlog を操作する"
	/>
</CardGrid>
